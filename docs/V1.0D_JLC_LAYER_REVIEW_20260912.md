# V1.0D JLC Gerber layer review — 2026-09-12

## Result

**Layer-recognition checkpoint: PASS.**

The JLC Gerber viewer now recognizes the release as the intended 4-layer PCB instead of treating copper as miscellaneous layers.

Recognized stack/function list:

1. Top silkscreen
2. Top paste
3. Top solder mask
4. Top copper / routing
5. Inner layer 1
6. Inner layer 2
7. Bottom copper / routing
8. Bottom solder mask
9. Board outline
10. Plated drill

This matches `manufacturing/V1.0D/LAYER_MAP.md` and is a clear improvement over V1.0C.

## Follow-up isolated-layer screenshots

Four screenshots were supplied for the requested isolated-layer sanity check.

### Inner layer 1 — PASS

The screenshot with only `内层1` plus board outline enabled shows a continuous solid copper plane over essentially the full board area. This is consistent with the intended L2 GND plane.

### Inner layer 2 — PASS

The screenshot with only `内层2` plus board outline enabled likewise shows a continuous solid copper plane. This is consistent with the intended L3 GND plane.

### Bottom copper — PASS

The screenshot with `底层线路` plus board outline and plated drill enabled shows a ground-dominated bottom copper layer with the expected via pattern and no RF signal-routing trace. This is consistent with the intended L4 ground copper / SMA ground structure.

### Top copper — NOT YET VERIFIED

The first supplied screenshot is **not** the isolated Top-copper view. Its visibility checkboxes show:

- `顶层丝印` enabled;
- `外型层` enabled;
- `通孔` enabled;
- `顶层线路` disabled.

Accordingly, that screenshot shows the red silkscreen arrows, plus sign and rectangular shield/reference outline, but it cannot be used to validate top-copper routing.

A final screenshot is still required with only:

- `顶层线路` enabled;
- `外型层` enabled;
- `通孔` optionally enabled;

and with `顶层丝印`, paste, mask, inner layers and bottom copper disabled.

The isolated Top-copper view must visibly contain:

- both SMA center signal lands;
- the complete input RF path from input SMA through C1 to U1;
- all QPL9547 SMT copper pads;
- the complete output RF path from U1 through C2 to the output SMA;
- the bias/supply traces and all SMT component copper pads.

## Current manufacturing status

- Gerber layer recognition: **PASS**
- Inner1 visual check: **PASS**
- Inner2 visual check: **PASS**
- Bottom-copper visual check: **PASS**
- Top-copper isolated visual check: **PENDING**

Do **not** pay/order yet. Remaining manufacturing checkpoints are:

1. isolated Top-copper viewer check described above;
2. live JLC stack-up / 50-ohm impedance calculation;
3. CAM/DFM review;
4. BOM/CPL and U1 pin-1 placement review;
5. confirmation of the two U1 via-in-pad holes and selected fill/cap process.
